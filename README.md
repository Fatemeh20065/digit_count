# digit_count
🌟 This program reads an integer from the user and calculates how many digits it has. It uses a recursive method digitcount that divides the number by 10 until only one digit remains. 🌟
